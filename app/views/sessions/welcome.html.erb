<h1>Welcome</h1>

<% if logged_in? %>
    <h1>You are Logged In, <%= current_cc_user.username || current_viewer_user.account_name%></h1>
<%end%>

<% if !logged_in? %>
<thead>
    
</thead>
<%= button_to "Login", '/login', method: :get %>

<%= button_to "Sign Up", '/content_creators/new', method: :get%>
  
<% end %>

<%# <table>
  <thead>
    <tr>
      <th>Title</th>
      <th>Description</th>
      <th>Hashtag</th>
      <th>Content creator</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @videos.each do |video| %>
      <%# <tr> %>
        <%# <td><%= video.title %></td> %>
        <%# <td><%= video.description %></td> %>
        <%# <td><%= video.hashtag %></td> 
        <%# <td><%= video.content_creator_id %> </td>  %>
        <%# <% end %> 
  <%# </tbody> %>
<%# </table> %> 

<br>